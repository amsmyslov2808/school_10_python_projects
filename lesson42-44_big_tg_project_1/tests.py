"""Небольшой ручной эксперимент с GeoNames API.

Это пока не автоматические тесты: файл выполняет настоящий сетевой запрос при
запуске. Переменная ``a`` получает список крупных городов около Брянска и может
быть просмотрена в отладчике; ``v`` служит простой точкой для breakpoint.
"""

from api.geonames_api import *

from models.city import City

# Сначала GeoNames находит координаты Брянска, затем по этим координатам ищет
# до пяти городов, подходящих по расстоянию и численности населения.
a = find_nearby_cities(find_city_by_name("Брянск"))

# Техническая переменная не влияет на приложение и оставлена для ручной отладки.
v = 10
